# Core Dependencies for Even/Odd League Player Agent
# These are required for the project to run

# Data processing
numpy>=1.21.0
pandas>=1.3.0

# HTTP Server & API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
httpx>=0.28.0

# AI & Agent Framework
agno>=0.59.0
google-generativeai>=0.8.0

# Configuration & Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.2
structlog>=24.4.0

# For development dependencies, install with: pip install -e ".[dev]"
# This will install additional dev dependencies from pyproject.toml:
# - pytest, pytest-asyncio, pytest-cov (testing)
# - black, flake8, mypy (code quality)
# - jupyter, matplotlib, seaborn, ipython (analysis)
